# CLAUDE.md

@../AGENTS.md
@AGENTS.md

Claude Code reads `CLAUDE.md`, not `AGENTS.md` directly. This Android-scoped file imports the root ERICK guidance plus Android-specific UI and IME rules.

## Claude Code Android Notes

- For Android UI work, explicitly check whether the layout needs to hold on both smaller and larger phone-sized screens.
- If you cannot run an emulator or device check, state the validation gap clearly.