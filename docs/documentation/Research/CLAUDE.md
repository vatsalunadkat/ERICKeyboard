# CLAUDE.md

@../../../AGENTS.md
@AGENTS.md

Claude Code reads `CLAUDE.md`, not `AGENTS.md` directly. This research-scoped file imports the root ERICK guidance plus the Python and reproducibility rules for the research subtree.

## Claude Code Research Notes

- For Python work, prefer reproducible scripts, explicit inputs and outputs, and targeted validation over vague “should work” claims.
- If a long optimizer run was not rerun, state that clearly instead of implying the reported metrics were refreshed.